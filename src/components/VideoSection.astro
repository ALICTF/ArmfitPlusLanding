---
// src/components/VideoSection.astro
---
<section id="video" class="py-20 px-4 relative z-10">
    
    <div class="max-w-5xl mx-auto bg-white rounded-[2.5rem] shadow-[0_20px_60px_-15px_rgba(37,99,235,0.1)] border border-slate-100 overflow-hidden relative group">
        
        <div class="absolute inset-0 opacity-[0.03] bg-[url('https://www.transparenttextures.com/patterns/medical-icons.png')]"></div>
        
        <div class="absolute top-0 left-0 w-64 h-64 bg-gradient-to-br from-blue-50 to-transparent rounded-br-[10rem] -ml-10 -mt-10 z-0 opacity-60"></div>

        <div class="relative z-10 py-12 flex flex-col items-center">

            <div class="text-center mb-12">
                <span class="text-blue-600 font-bold text-[10px] bg-blue-50 px-3 py-1 rounded-full border border-blue-100 animate-pulse inline-flex items-center gap-1">
                    <span class="relative flex h-1.5 w-1.5">
                      <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                      <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-blue-600"></span>
                    </span>
                    آموزش ویدیویی
                </span>
                <h3 class="text-3xl font-black text-slate-900 mt-4 mb-2">دستیار هوشمند قلب شما</h3>
                <p class="text-sm text-slate-500">برای مشاهده نحوه کارکرد آسان دستگاه، روی موبایل زیر ضربه بزنید</p>
            </div>
            
            <div class="relative max-w-[320px] mx-auto">
                
                <div class="absolute inset-0 bg-gradient-to-tr from-blue-200 via-cyan-100 to-purple-200 blur-[60px] -z-10 rounded-full scale-110 opacity-80"></div>

                <div class="absolute top-16 -right-12 z-20 animate-float" style="animation-duration: 4s;">
                    <div class="bg-white/90 backdrop-blur shadow-lg border border-blue-100 p-2 rounded-2xl flex items-center gap-2">
                        <div class="bg-blue-50 p-1.5 rounded-xl"><svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg></div>
                        <span class="text-[9px] font-bold text-slate-700">AI Connected</span>
                    </div>
                </div>

                <div class="relative rounded-[2.5rem] border-4 border-white bg-white p-1 shadow-2xl ring-1 ring-slate-200/60 overflow-hidden group transform hover:scale-[1.01] transition-transform duration-500">
                    
                    <div class="rounded-[2rem] overflow-hidden relative bg-slate-900 aspect-[9/16] isolate">
                        
                        <video 
                            id="promo-video"
                            playsinline
                            preload="metadata"
                            class="w-full h-full object-cover"
                        >
                            <source src="/ArmfitPlusLanding/video.mp4#t=0.1" type="video/mp4" />
                            مرورگر شما پشتیبانی نمی‌کند.
                        </video>

                        <div id="video-overlay" class="absolute inset-0 flex flex-col items-center justify-center bg-slate-900/30 backdrop-blur-[2px] cursor-pointer transition-opacity duration-500 z-20 hover:bg-slate-900/20">
                            
                            <div class="w-20 h-20 bg-white/20 backdrop-blur-md rounded-full flex items-center justify-center border border-white/40 shadow-lg group-hover:scale-110 transition-transform duration-300">
                                <div class="w-14 h-14 bg-blue-600 rounded-full flex items-center justify-center shadow-inner text-white">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 ml-1" viewBox="0 0 24 24" fill="currentColor">
                                        <path d="M8 5v14l11-7z" />
                                    </svg>
                                </div>
                            </div>
                            
                            <span class="text-white text-xs font-bold mt-4 animate-pulse drop-shadow-md">تماشای ویدیو</span>
                        </div>

                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<script>
    const video = document.getElementById('promo-video');
    const overlay = document.getElementById('video-overlay');

    if (video && overlay) {
        overlay.addEventListener('click', () => {
            video.play(); 
            video.controls = true; 
            overlay.style.opacity = '0'; 
            setTimeout(() => {
                overlay.style.display = 'none'; 
            }, 500);
        });

        video.addEventListener('pause', () => {
             if (!video.seeking) {
                overlay.style.display = 'flex';
                setTimeout(() => overlay.style.opacity = '1', 10);
                video.controls = false; 
             }
        });
        
        video.addEventListener('play', () => {
             overlay.style.opacity = '0';
             setTimeout(() => overlay.style.display = 'none', 500);
             video.controls = true;
        });
    }
</script>